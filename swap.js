let a = 18;
let b = 3;
[a,b]=[b, a];
console.log(` value of a is ${a} and b is ${b}`);
  // with third variable
  // let temp = a
  // a=b
  // b=temp
  // console.log(` value of a is${a} and b is ${b}`)

  // without third variable

 
